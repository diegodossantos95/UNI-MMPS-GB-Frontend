<mvc:View
		controllerName="com.diegodossantos95.MMPSFrontend.controller.Detail"
		xmlns:mvc="sap.ui.core.mvc"
	    xmlns:form="sap.ui.layout.form"
        xmlns:core="sap.ui.core"
		displayBlock="true"
		xmlns="sap.m">
    <Page id="detailPage" title="Details">
        <content>
            <form:SimpleForm editable="true">
                <form:content>
                    <Label text="Sigla"/>
                    <Input value="{Detail>/initials}" visible="{path: 'Detail>/initials', formatter:'.formatter.isAvailable'}"/>
                    
                    <Label text="Name"/>
                    <Input value="{Detail>/name}"/>
                    
                    <Label text="Description"/>
                    <Input value="{Detail>/description}" visible="{path: 'Detail>/description', formatter:'.formatter.isAvailable'}"/>
                    
                    <Label text="Category"/>
                    <Select selectedKey="{Detail>/category/id}" items="{Relationship>/category}" visible="{path: 'Detail>/category', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                    
                    <Label text="Specific Practice"/>
                    <Select selectedKey="{Detail>/specific-practice/id}" items="{Relationship>/specific-practice}" visible="{path: 'Detail>/specific-practice', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                    
                    <Label text="Specific Goal"/>
                    <Select selectedKey="{Detail>/specific-goal/id}" items="{Relationship>/specific-goal}" visible="{path: 'Detail>/specific-goal', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                    
                    <Label text="Generic Goal"/>
                    <Select selectedKey="{Detail>/generic-goal/id}" items="{Relationship>/generic-goal}" visible="{path: 'Detail>/generic-goal', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                    
                    <Label text="Capacity Level"/>
                    <Select selectedKey="{Detail>/capacity-level/id}" items="{Relationship>/capacity-level}" visible="{path: 'Detail>/capacity-level', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                    
                    <Label text="Process Area"/>
                    <Select selectedKey="{Detail>/process-area/id}" items="{Relationship>/process-area}" visible="{path: 'Detail>/process-area', formatter:'.formatter.isAvailable'}">
                      <core:Item key="{Relationship>id}" text="{Relationship>name}" />
                    </Select>
                </form:content>
            </form:SimpleForm>
        </content>
        <footer>
			<Toolbar>
                <Button text="Delete" press="onDeletePress"/>
				<ToolbarSpacer/>
				<Button text="Cancel" press="onCancelPress"/>
                <Button text="Save" press="onSavePress"/>
			</Toolbar>
		</footer>
    </Page>
</mvc:View>
